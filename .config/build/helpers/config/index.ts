import { Configuration } from 'webpack';

export const config: Configuration = {
  entry: {},
  module: { rules: [] },
  optimization: {},
  output: {},
  plugins: [],
  resolve: { alias: {}, extensions: ['.js'] },
};
