import { validate } from '@micra/validator';
import { required } from '@micra/validation-rules';
import type { {{{ NAME.PASCAL }}} } from 'domains/{{{ DOMAIN.SINGULAR.KEBAB }}}/types/common/{{{ NAME.PASCAL }}}';

export const {{{ NAME.CAMEL }}} = (input: Record<string, any>) => validate<{{{ NAME.PASCAL }}}>(input, {
  // TODO: implement {{{ NAME.PASCAL }}} validation rules.
});
